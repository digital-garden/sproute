extends layout

block content
  h1= title
  p Welcome to #[em LocalLibrary], a very basic Express website developed as a tutorial example on the Mozilla Developer Network.


  h1 Dynamic content

  if error
    p Error getting dynamic content.
  else

    p The library has the following record counts:

    ul
      li #[strong Plants:] !{data.plant_count}
      li #[strong Copies:] !{data.plant_instance_count}
      li #[strong Copies available:] !{data.plant_instance_available_count} 
      li #[strong Clippings:] !{data.clipping_count}
      li #[strong Speciess:] !{data.species_count}